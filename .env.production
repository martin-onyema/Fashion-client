# ─────────────────────────────────────────────────────────────
# Wardrobecare Clothing — PRODUCTION environment
# ─────────────────────────────────────────────────────────────
# This file ships with the deployment and is loaded automatically
# by Next.js during `next build` and at runtime on Vercel.
# You do NOT need to paste anything into the Vercel dashboard —
# it works out of the box. (If you later add env vars in the
# Vercel dashboard, those take precedence over this file.)
#
# NOTE: keep this file private. If you push the project to
# GitHub, push to a PRIVATE repository.
# ─────────────────────────────────────────────────────────────

# Supabase PostgreSQL (transaction pooler — pgbouncer, serverless-safe)
DATABASE_URL=postgresql://postgres.uvnuhhazklixymhtcshq:mm4you,,A..@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&prepared_statements=false

# Same pooler, session mode (5432) — used by Prisma CLI for db push / migrations
DIRECT_URL=postgresql://postgres.uvnuhhazklixymhtcshq:mm4you,,A..@aws-1-eu-west-1.pooler.supabase.com:5432/postgres

# NextAuth
NEXTAUTH_SECRET=hhzNdctvn9smxilpARymKf9I9QDcSx6rm22VJ5m2BUY=
NEXTAUTH_URL=https://fashion-client-7npv.vercel.app

# ── After connecting a custom domain (e.g. https://wardrobecare.com.ng),
# ── update NEXTAUTH_URL above and redeploy.

# Resend — signup verification codes (send-only key)
RESEND_API_KEY=re_KBLC1fbu_FTzGZerDxLuGZuHus9jY47M7
