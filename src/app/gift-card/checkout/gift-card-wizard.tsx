"use client";

/**
 * Replacement for a leftover file from an older project version.
 * Fully self-contained so Vercel builds can never fail on it.
 */
export function GiftCardWizard() {
  return (
    <div className="mx-auto max-w-md px-6 py-16 text-center">
      <p className="text-xs uppercase tracking-[0.3em] text-neutral-500">
        Wardrobecare
      </p>
      <h1 className="mt-4 text-3xl font-semibold">Gift Cards</h1>
      <p className="mt-3 text-sm text-neutral-600">
        Wardrobecare gift cards are launching soon. Please check back
        shortly.
      </p>
    </div>
  );
}

export default GiftCardWizard;
